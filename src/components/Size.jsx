const Size = ({size}) => {
  return (
    // <select>
    //   <option selected>&nbsp;</option>
    //   {size.length > 0 && size.map((item, index) => (
    //     <option value={item} key={index}>{item}</option>
    //   ))}
    // </select>
    <div className="size">
      
    </div>
  )
}

export default Size